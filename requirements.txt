# Versions minimales plutot que figees : le service d'hebergement choisit
# une version compatible avec son interpreteur Python.
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
jinja2>=3.1.5
python-multipart>=0.0.20
psycopg[binary]>=3.2.3
python-dotenv>=1.0.1
reportlab>=4.2.0
# Base des fuseaux horaires : permet d'afficher l'heure de Paris meme quand
# le systeme ne fournit pas la base (Windows, images Linux minimales).
tzdata>=2024.1
